# ============================================================
# 3SOC - Traffic Violation Detection System
# Requirements file - Python 3.10+
# ============================================================

# --- Web Framework ---
fastapi==0.128.0              # Framework REST API chinh
uvicorn[standard]==0.40.0     # ASGI server chay FastAPI (bao gom websockets, watchfiles, httptools)

# --- AI / Computer Vision ---
ultralytics==8.3.251          # YOLOv8/v11 - framework load model .pt va chay inference
opencv-python==4.12.0.88      # Doc video, decode frame, xu ly anh
numpy==2.2.6                  # Xu ly mang so, thao tac pixel frame
pillow==12.1.0                # Doc/ghi anh (YOLO render ket qua detection)

# --- Database ---
sqlalchemy==2.0.45            # ORM - map Python class <-> bang MySQL
pymysql==1.1.2                # MySQL driver thuan Python cho SQLAlchemy
mysql-connector-python==9.5.0 # Driver ket noi MySQL (alternative connector)

# --- Authentication & Security ---
python-jose[cryptography]==3.5.0  # Tao va xac thuc JWT (HS256)
passlib[argon2]==1.7.4            # Framework hash mat khau (dung thuat toan argon2)
argon2-cffi==25.1.0               # Backend Argon2 cho passlib (thuat toan hash manh nhat hien tai)
cryptography==46.0.3              # Thu vien mat ma cho python-jose

# --- Request Handling ---
python-multipart==0.0.21      # Xu ly multipart/form-data (upload file qua API)
websockets==16.0              # Ho tro WebSocket protocol

# --- Data Validation ---
pydantic[email]==2.12.5       # Validation request/response schema (tich hop san FastAPI)
email-validator==2.3.0        # Validate dinh dang email trong Pydantic

# --- Environment ---
python-dotenv==1.2.1          # Doc bien moi truong tu file .env

# --- System Monitoring ---
psutil==7.2.1                 # Lay thong so CPU%, RAM% broadcast qua WebSocket

# --- Utilities ---
requests==2.32.5              # HTTP client (dung boi ultralytics de download model)
scipy==1.16.3                 # Tinh toan khoa hoc (dependency cua ultralytics)
matplotlib==3.10.8            # Ve bieu do/plot (dependency cua ultralytics)
